What is the primary benefit of the Infrastructure as Code (IaC) principle of 'Consistency'?
R: It ensures that the same configuration results in identical environments across different stages.

Terraform manages multi-cloud workflows by using a single _____ to interact with various cloud providers.
R: Configuration language (HCL)

Which specific file is generated by Terraform to ensure that every team member uses the exact same provider versions?
R: The dependency lock file (.terraform.lock.hcl)

In a provider block, what is the purpose of the 'alias' argument?
R: It allows a configuration to use multiple configurations for the same provider, such as different regions.

What is the correct syntax for referencing a provider with an alias of 'west' in a resource block?
R: provider = aws.west


True or False: Terraform providers are distributed as plugins that are separate from the Terraform CLI binary.
R: True

Which command must be executed to download and install the providers specified in a configuration?
tR: erraform init

If you want to update the dependency lock file to allow newer provider versions within your constraints, which flag should you use with 'terraform init'?
R: -upgrade

What is the function of 'Terraform State' regarding metadata?
R: It tracks the mapping between resources in your configuration and the real-world IDs provided by the infrastructure.

Why does Terraform require a state file to perform a 'terraform destroy'?
R: The state file contains the mapping of managed resources to their actual cloud provider IDs.

Why does Terraform require a state file to perform a 'terraform destroy'?
R: The state file contains the mapping of managed resources to their actual cloud provider IDs.

Which command is used to verify that the syntax of the Terraform configuration is correct without accessing remote services?
R: terraform validate

What effect does 'terraform plan' have on the actual cloud infrastructure?
R: None; it only creates an execution plan and refreshes the state.

Which flag allows 'terraform apply' to execute without requiring manual user confirmation?
R: -auto-approve

To remove all resources managed by a specific Terraform workspace, which command should be used?
R: terraform destroy

Which command automatically adjusts the indentation and spacing of your .tf files to match HashiCorp standards?
R: terraform fmt

What is the fundamental difference between a 'resource' block and a 'data' block?
R: Resource blocks create and manage infrastructure, while data blocks fetch information from existing infrastructure.

Which argument is mandatory when defining a Terraform 'output' block?
R: value

How does setting 'sensitive = true' on an input variable affect the Terraform state file?
R: It does not protect the value in the state file; it only masks the value in CLI output.

When using a child module, how can the parent module access specific information from the child's resources?
R: The child module must define an 'output' block for that information.

Which meta-argument is used to handle resource creation order when Terraform cannot automatically detect a dependency?
R: depends_on

The _____ lifecycle argument ensures a new resource is functional before the old one is deleted during an update.
R: create_before_destroy

New to Terraform 004, which block allows you to define functional validation for infrastructure that is checked after 'apply'?
R: check

What is the purpose of 'ephemeral' values in Terraform configuration?
R: They handle short-lived, sensitive data that should not be persisted in the state file.

What happens if a module version is omitted when sourcing a module from the Terraform Registry?
R: Terraform will automatically download the latest available version.

True or False: A child module can directly access any variable defined in the parent module's root configuration.
R: False

Which feature prevents two users from running 'terraform apply' on the same state file simultaneously?
R: State locking

What is the primary risk of using a 'local' backend in a team environment?
R: Lack of collaborative access and the absence of state locking, leading to state corruption.

Which command removes a resource from the state file but does not delete the actual resource in the cloud?
R: terraform state rm

How does 'terraform apply -refresh-only' differ from a standard 'terraform refresh'?
R: It allows the user to review the changes to the state file and optionally commit them, rather than updating state immediately.

When a resource exists in the cloud but is not present in the state file, the situation is known as _____.
R: Drift

Which command is used to bring an existing cloud resource under Terraform management?
R: terraform import

After running 'terraform import', what manual action must the user take to complete the management process?
R: The user must manually write the matching resource configuration block in a .tf file.

Which environment variable is used to enable detailed debugging output in the Terraform CLI?
R: TF_LOG

Which logging level provides the most verbose output for troubleshooting Terraform issues?
R: TRACE

In HCP Terraform, what is the organizational level used to group related workspaces and manage their access collectively?
R: Projects

How does an HCP Terraform 'Workspace' differ from a CLI 'Workspace'?
R: HCP workspaces contain state, variables, and history for a specific environment, whereas CLI workspaces are primarily for local state separation.

Which HCP Terraform feature allows a change in one workspace to automatically trigger a run in another workspace?
R: Run triggers

Which HCP Terraform feature allows you to apply the same environment variables or Terraform variables to multiple workspaces?
R: Variable sets

Which tool is used in HCP Terraform to enforce compliance and security guardrails through 'Policy as Code'?
R: Sentinel (or OPA)

Where does HCP Terraform's 'Remote Operations' execute the actual Terraform commands?
R: On HashiCorp-managed infrastructure (the HCP cloud runners).

Which command allows you to retrieve the API token required to authenticate the CLI with HCP Terraform?
R: terraform login

True or False: Terraform modules must be publicly accessible on the internet to be used.
R: False

What is the function of the 'moved' block in Terraform?
R: It allows for refactoring resources (renaming them) in configuration without destroying and recreating them in the state.

Which command would you use to see the detailed attributes of a single resource currently stored in the state?
R: terraform state show [resource_address]

What type of backend is required for HCP Terraform to perform remote operations?
R: cloud (or remote in older versions)

Which Terraform configuration element is used to fetch a password from HashiCorp Vault?
R: A data block using the Vault provider.

A collection of Terraform configuration files in a directory is referred to as a _____.
R: Module

What is the purpose of the 'terraform graph' command?
R: It generates a visual representation of the dependency relationships between resources.

In the core Terraform workflow, which step follows 'Plan'?
R: Apply

Which argument within a 'variable' block is used to ensure the input meets specific criteria before execution?
R: validation


